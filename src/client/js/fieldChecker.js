function validateText(inputText) {
    // console.log(`Running validateText on ${inputText}`);
    if (inputText==""){
        return false;
    } else {
        return true;
    }
}
export { validateText }
